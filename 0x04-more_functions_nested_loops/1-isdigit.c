#include "main.h"
/**
 * _isdigit - if ia a digit[D[D[D[D[D[D[D[s[C[C[C[C[C[C[C[C
 *@C: print int
 * Return: Always 0.
 */

int _isdigit(int c)
{
if (c >= 48 && c <= 57)
{
return (1);
}

else
{
return (0);
}
}
